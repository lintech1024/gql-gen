query GetProcess($processId: ID!){
    getProcess(processId: $processId){
        id
        name
        serviceId
        serviceName
        instanceId
        instanceName
        agentId
        detectType
        attributes{
            name
            value
        }
        labels
    }
}