query GetAllServices($duration: Duration!, $group: String){
    getAllServices(duration: $duration, group: $group){
        id
        name
        group
        shortName
        layers
        normal
    }
}